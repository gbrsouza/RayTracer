#include "integrators/samplerIntegrator.h"

void 
SamplerIntegrator::render(
    const Scene& scene )
{
    /**
     * This might be just an empty method,
     * or not, depending on the integrator's
     * needs.
     */
    preprocess( scene );

    // @TODO

}